// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    ext.kotlinVer = '1.2.10'
    ext.ankoVer = '0.10.3'

    repositories {
        google()
        jcenter()
        maven { url 'https://maven.fabric.io/public' }
    }

    dependencies {
        classpath 'com.android.tools.build:gradle:3.0.1'
        classpath 'com.google.gms:google-services:3.1.2' // Google Services Gradle Plugin
        classpath 'io.fabric.tools:gradle:1.25.1' // Fabric's Gradle Plugin for Crashlytics
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVer" // Kotlin Gradle Plugin
    }

}

allprojects {
    repositories {
        google()
        jcenter()
        maven { url "https://jitpack.io" }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
